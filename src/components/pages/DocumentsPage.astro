---
import type { Locale } from '../../i18n';
import { t } from '../../i18n';
import Layout from '../../layouts/Layout.astro';

interface Props {
  locale: Locale;
}

interface Document {
  name: string;
  description: string;
  file: string;
  date: Date;
}

const { locale } = Astro.props;

const downloadHeading = t(locale, 'documents.downloadHeading');
const downloadText = t(locale, 'documents.download');

const documents: Document[] = [
  {
    name: t(locale, 'documents.list.0.name'),
    description: t(locale, 'documents.list.0.description'),
    file: t(locale, 'documents.list.0.file'),
    date: new Date('2025-08-31'),
  },
];
---

<Layout
  title={t(locale, 'documents.title')}
  description={t(locale, 'documents.description')}
  activeNav="documents"
>
  <main class="container">
    <div class="documents-container">
      <h2 class="text-heading-1">{downloadHeading}</h2>

      <ul class="documents-list">
        {
          documents.map((doc, index) => (
            <li class="document-item">
              <div class="document-header">
                <div class="header-details">
                  <span class="document-name text-body-medium">{doc.name}</span>
                  <p class="document-date text-body">
                    {doc.date.toLocaleDateString(locale, {
                      year: 'numeric',
                      month: 'long',
                      day: 'numeric',
                    })}
                  </p>
                </div>
                <a href={doc.file} class="document-download text-body-medium" download>
                  {downloadText}
                </a>
              </div>

              <p class="document-description text-body">{doc.description}</p>
              {index < documents.length - 1 && <hr class="document-divider" />}
            </li>
          ))
        }
      </ul>
    </div>
  </main>
</Layout>

<style>
  .documents-container {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .documents-list {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .document-item {
    display: flex;
    flex-direction: column;
    gap: var(--space-1);
  }

  .header-details {
    display: flex;
    flex-direction: column;
  }

  .document-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: var(--space-2);
  }

  .document-name {
    flex: 1;
    color: var(--color-text-primary);
  }

  .document-download {
    white-space: nowrap;
    text-decoration: underline;
    color: var(--color-text-secondary);
    cursor: pointer;
    transition: color var(--transition-fast);
  }

  .document-download:hover {
    color: var(--color-text-primary);
  }

  .document-date {
    margin: 0;
    color: var(--color-text-secondary);
  }

  .document-description {
    margin: 0;
    color: var(--color-text-primary);
  }

  .document-divider {
    border: none;
    border-top: var(--border-width-thin) solid var(--color-border-light);
    margin-top: var(--space-4);
  }
</style>
